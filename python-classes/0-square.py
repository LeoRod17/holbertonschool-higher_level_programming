#!/usr/bin/python3
"""python3 -c 'print(__import__("my_module").__doc__)'
this is a task for holberton school were i have to creat an empty class
because we are starting working with OOP(Object Oriented Programming)
"""


class Square:
    """(python3 -c 'print(__import__("my_module").MyClass.__doc__)')
    This is an empty class of a square to practise in how we create a
    empty class without methods and attributes
    the pass it is for saying that it is an empty function and the class
    is square because that is the class that they asked
    """
    pass
